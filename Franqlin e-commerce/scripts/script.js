function navigateTo(page) {
    if (page === 'login') {
      window.location.href = '/login.html'; // Update to your actual login page
    } else if (page === 'register') {
      window.location.href = '/register.html'; // Update to your actual register page
    }
  }
  